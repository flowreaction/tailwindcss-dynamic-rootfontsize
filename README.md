# tailwindcss-dynamic-rootfontsize README

## usage
create a mapping of font sizes with glob patterns where they should be applied

```json
{
  "tailwindcss-dynamic-rootfontsize": {
    "16": ["**/*.html", "**/*.css"],
    "18": ["**/src/**/*.html", "**/src/**/*.css"]
  }
}
```
{

}